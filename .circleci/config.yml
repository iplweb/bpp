version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout

      - run:
          name: Pull docker images from hub
          command: docker-compose pull
          
      - run:
          name: Boot docker 
          command: make docker-up

      - run:
          name: docekr-assets
          command: make docker-assets

      - run:
          name: Python tests
          command: make docker-python-tests
          
      - run:
          name: JS tests
          command: make docker-js-tests

