from django.apps import AppConfig


class DeduplikatorPublikacjiConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "deduplikator_publikacji"
    verbose_name = "Deduplikator publikacji"
