# miniblog.Article was retired in miniblog/migrations/0004 — the model now
# lives in `django-site-blog` (`siteblog.Article`). This app stays in
# INSTALLED_APPS only so Django's migration loader can walk the history.
