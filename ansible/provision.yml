---
- hosts: bpp-db
  become: yes
  become_method: sudo
  vars:
    database_name: bpp
    database_user: bpp
    database_password: password
    postgresql_version: 9.3
  roles: 
    - postgresql-server

- hosts: bpp-master
  become: yes
  become_method: sudo
  roles:
    - nginx-with-pubsub
    - name: nginx-messaging-test
      tags: ['fo']
